name: 'Terraform Landing Zone Action' # Renamed for clarity

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Clean local Terraform caches
        run: rm -rf .terraform*

      - name: Terraform Init
        id: init
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TERRAFORM_STATE_BUCKET }}"

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -no-color \
            -var="org_id=${{ secrets.GCP_ORG_ID }}" \
            -var="billing_account=${{ secrets.GCP_BILLING_ACCOUNT }}" \
            -var-file="terraform.tfvars.json"

      - name: Terraform Apply
        id: apply
        run: |
          terraform apply -auto-approve -no-color \
            -var="org_id=${{ secrets.GCP_ORG_ID }}" \
            -var="billing_account=${{ secrets.GCP_BILLING_ACCOUNT }}" \
            -var-file="terraform.tfvars.json"

      # --- STEP 3: NEW DESTROY STEP IS ADDED ---
      # This will now run instead of plan and apply.
      # - name: Terraform Destroy
      #   id: destroy
      #   run: |
      #     terraform destroy -auto-approve -no-color \
      #       -var="org_id=${{ secrets.GCP_ORG_ID }}" \
      #       -var="billing_account=${{ secrets.GCP_BILLING_ACCOUNT }}" \
      #       -var-file="terraform.tfvars.json"

