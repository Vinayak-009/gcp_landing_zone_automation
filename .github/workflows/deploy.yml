name: 'Terraform Landing Zone Deploy'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      #id-token: 'write' # Required if using Workload Identity (Optional but recommended)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Clean local Terraform caches
        run: rm -rf .terraform*

      - name: Terraform Init
        id: init
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TERRAFORM_STATE_BUCKET }}"

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -no-color \
            -var="org_id=${{ secrets.GCP_ORG_ID }}" \
            -var="billing_account=${{ secrets.GCP_BILLING_ACCOUNT }}" \
            -var-file="terraform.tfvars.json"

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        id: apply
        run: |
          terraform apply -auto-approve -no-color \
            -var="org_id=${{ secrets.GCP_ORG_ID }}" \
            -var="billing_account=${{ secrets.GCP_BILLING_ACCOUNT }}" \
            -var-file="terraform.tfvars.json"
